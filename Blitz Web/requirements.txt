# ── Core backend ──────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
groq>=0.5.1
tavily-python>=0.3.1
python-dotenv>=1.0.1
pydantic>=2.7.1
httpx>=0.27.0
psutil>=5.9.8
Pillow>=10.3.0

# ── Auth ──────────────────────────────────────────────────
python-jose[cryptography]>=3.3.0

# ── Vector Memory ─────────────────────────────────────────
pinecone-client>=4.0.0

# ── PC Bridge ─────────────────────────────────────────────
websockets>=12.0

# ── Windows-only (system control — auto-skipped on Linux) ─────
pywin32>=306 ; sys_platform == 'win32'
pycaw>=20240210 ; sys_platform == 'win32'
comtypes>=1.4.1 ; sys_platform == 'win32'
pyautogui>=0.9.54 ; sys_platform == 'win32'
screen-brightness-control>=0.23.0 ; sys_platform == 'win32'

# ── Voice Engine ──────────────────────────────────────────
edge-tts>=6.1.12
