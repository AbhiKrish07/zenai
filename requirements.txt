# ── Core backend ──────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
groq>=0.5.0
tavily-python>=0.3.0
python-dotenv>=1.0.0
pydantic>=2.0.0
httpx>=0.27.0      # used by ElevenLabs TTS + Jina AI embeddings
psutil>=5.9.0
Pillow>=10.0.0

# ── Auth ──────────────────────────────────────────────────
python-jose[cryptography]>=3.3.0

# ── Vector Memory (optional — uncomment when PINECONE_API_KEY is set) ─────
# Jina AI embeddings are called via httpx (already listed above) — no extra package
# pinecone-client>=3.0.0

# ── PC Bridge ─────────────────────────────────────────────
websockets>=12.0

# ── Windows-only (system control — DO NOT uncomment for Render/Linux) ─────
# pywin32>=306
# pycaw>=20240210
# comtypes>=1.4.1
# pyautogui>=0.9.54
# screen-brightness-control>=0.23.0
